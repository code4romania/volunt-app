<tr>
  <td><%= project_row.name %></td>
  <td><%= project_row.owner.full_name unless project_row.owner.nil? %></td>
  <td><%= render partial: 'shared/tags', locals: {tags: project_row.tags} %></td>
  <td><%= glyphicon_link_to project_path(project_row) %></td>
</tr>
