<div class="container">
  <div class="row">
    <%= render partial: 'shared/notice' %>
    <div class="btn-group" role="group">
      <%= link_to 'Adauga Voluntar', new_volunteer_path, class: 'btn btn-default' %>
    </div>
  </div>
  <div class="row">
    <%= render partial: 'shared/profiles_table', locals: {profiles: @volunteers} %>
    <%= will_paginate @volunteers, renderer: BootstrapPagination::Rails %>
  </div>
</div>
