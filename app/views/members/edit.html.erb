<div class="container">
  <%= render 'form', member: @member do |f|%>
		<div class="form-group">
			<div class="col-sm-9 col-sm-offset-3">
				<hr/>
				<input type="submit" class="btn btn-primary" value="Save" />
        <%= link_to 'Cancel', project_member_path(@project, @member), class: 'btn btn-default' %>
      </div>
    </div>
  <% end %>
</div>
